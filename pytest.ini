#Keep test configs consistent (logs, report paths, etc.).
#统一测试配置（日志、报告路径等）。

[pytest]
addopts = -q -ra --maxfail=1 --disable-warnings
testpaths = tests

#增强（让偶发 UI 抖动更稳）
addopts = -q -ra --maxfail=1 --disable-warnings --reruns 2 --reruns-delay 2

